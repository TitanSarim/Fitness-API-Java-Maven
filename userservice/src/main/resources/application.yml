spring:
  application:
    name: userservice
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
