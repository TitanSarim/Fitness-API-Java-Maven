spring:
  application:
    name: activity-service
  data:
    mongodb:
      uri: 
      database: 

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
