spring:
  application:
    name: activity-service
  data:
    mongodb:
      uri: mongodb+srv://sarimxahid:1412@cluster0.kaxoprv.mongodb.net/fitnessactivity
      database: fitnessactivity

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/